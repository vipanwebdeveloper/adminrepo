Hi {{ userEntity.firstName }},
        
Someone requested that the password be reset for your {%if app.session.get('loginDisplayName')%}{{app.session.get('loginDisplayName')}}{%else%}pulsetracker{%endif%} account.

If this was a mistake, just ignore this email and nothing will happen.

To reset your password, visit the following address:
        
{{ url('reset_password', {'passwordResetCode' : userEntity.passwordResetCode }) }}
     